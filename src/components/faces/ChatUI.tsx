import { Thread } from "../assistant-ui/thread";

const ChatUI = () => {
    return (
        <div className={`chat-page cube-face`}>
            <Thread />
        </div>
    )
};

export default ChatUI;