import { Thread } from "../assistant-ui/thread";

const ChatUI = () => {
    return (
        <div className="chat-page h-[600px] w-[600px]">
            <Thread />
        </div>
    )
};

export default ChatUI;