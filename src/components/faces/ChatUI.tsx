import { Thread } from "../assistant-ui/thread";

const ChatUI = () => {
    return (
        <div className="chat-page h-[400px] w-[400px]">
            <Thread />
        </div>
    )
};

export default ChatUI;